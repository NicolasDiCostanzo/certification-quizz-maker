{
  "name": "certification-quizz-maker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -b --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "VITE_E2E_FAKE_AUTH=true VITE_COGNITO_USER_POOL_ID=e2e-fake-pool VITE_COGNITO_CLIENT_ID=e2e-fake-client VITE_SYNC_API_URL= npm run build && playwright test",
    "test:e2e:ui": "VITE_E2E_FAKE_AUTH=true VITE_COGNITO_USER_POOL_ID=e2e-fake-pool VITE_COGNITO_CLIENT_ID=e2e-fake-client VITE_SYNC_API_URL= npm run build && playwright test --ui",
    "deploy:site": "npm run build && : \"${STACK_NAME:?Set STACK_NAME to your CloudFormation stack name}\" && BUCKET=$(aws cloudformation describe-stacks --stack-name \"$STACK_NAME\" --query \"Stacks[0].Outputs[?OutputKey=='SiteBucketName'].OutputValue\" --output text) && aws s3 sync dist/ \"s3://$BUCKET\" --delete && aws cloudfront create-invalidation --distribution-id $(aws cloudformation describe-stacks --stack-name \"$STACK_NAME\" --query \"Stacks[0].Outputs[?OutputKey=='SiteDistributionId'].OutputValue\" --output text) --paths '/*'",
    "prepare": "husky",
    "knip": "knip --exclude cycles --no-progress"
  },
  "dependencies": {
    "aws-amplify": "^6.20.0",
    "pinia": "^4.0.3",
    "pinia-plugin-persistedstate": "^4.7.1",
    "vue": "^3.5.41",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1127.0",
    "@aws-sdk/lib-dynamodb": "^3.1127.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.5.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/eslint-config-typescript": "^14.9.0",
    "@vue/test-utils": "^2.5.0",
    "@vue/tsconfig": "^0.9.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "eslint-plugin-vue": "^10.10.0",
    "husky": "^9.1.7",
    "jsdom": "^30.0.1",
    "knip": "^6.35.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0",
    "vue-tsc": "^3.3.11"
  }
}
