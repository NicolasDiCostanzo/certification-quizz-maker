# Vite only exposes variables prefixed with VITE_ to the client bundle.
# These identifiers are public by design (they ship to every browser);
# never put secrets here. Copy to .env.local for real values.
VITE_AWS_REGION=
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_SYNC_API_URL=